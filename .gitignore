# Build output (prd §12).
.claude/bin/

# Per-worktree nudge state. Never shared, never committed (prd §5.2, §8).
.dlog/state/

# Journals are a per-repo choice (prd §6.2). dlog's own repo does not commit them;
# `dlog journal --export <session>` shares one when it is worth sharing.
.dlog/journal/
